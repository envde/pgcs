-- Файл с различными спецсимволами и кодировками

CREATE TABLE special_chars
(
    id SERIAL PRIMARY KEY,
    -- Кириллица
    name_ru VARCHAR(255) DEFAULT 'Привет мир',
    -- Эмодзи
    emoji_col TEXT DEFAULT '😀🎉🚀',
    -- Специальные символы
    special VARCHAR(255) DEFAULT 'Special: @#$%^&*()[]{}|<>?',
    -- Unicode
    unicode_col TEXT DEFAULT '中文字符',
    -- Табуляция	и пробелы
    tabs_spaces TEXT DEFAULT '	   ',
    -- Переносы строк
    newlines TEXT DEFAULT E'line1\nline2\rline3'
);

-- Комментарий с emoji: 🔥💯✨
COMMENT ON TABLE special_chars IS 'Таблица с различными спецсимволами 🎯';
-- Пустой файл (только комментарии)
-- Этот файл не содержит SQL определений
-- Используется для проверки корректной обработки пустых файлов

